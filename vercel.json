{
    "buildCommand": "cd olipop-nextjs && npm install && npm run build",
    "outputDirectory": "olipop-nextjs/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/olipop-nextjs/$1"
        }
    ]
}